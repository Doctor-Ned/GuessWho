namespace GuessWhoResources {
    public enum ResourceType {
        Locale,             // Contains UI labels
        CustomCategories,   // Contains custom category labels
        League              // Contains DataDragon-based League related labels (these resources are generated entirely by code)
    }
}
